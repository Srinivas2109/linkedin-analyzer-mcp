{
  "name": "linkedin-enhancer",
  "version": "1.0.0",
  "description": "LinkedIn Enhancer for Claude Desktop",
  "main": "index.js",
  "type": "module",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build": "tsc",
    "start": "node build/index.js",
    "build:client": "cd client && npx webpack",
    "build:server": "cd server && npx tsc"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.4.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
